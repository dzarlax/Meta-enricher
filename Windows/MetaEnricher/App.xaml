<Application
    x:Class="MetaEnricher.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:MetaEnricher.Converters">

    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Brushes -->
            <SolidColorBrush x:Key="AmberBrush" Color="#FFB938" />
            <SolidColorBrush x:Key="BackgroundBrush" Color="#1A1A1A" />
            <SolidColorBrush x:Key="SidebarBrush" Color="#242424" />
            <SolidColorBrush x:Key="SurfaceBrush" Color="#2D2D2D" />
            <SolidColorBrush x:Key="BorderBrush" Color="#3A3A3A" />
            <SolidColorBrush x:Key="TextPrimaryBrush" Color="#F5F5F5" />
            <SolidColorBrush x:Key="TextSecondaryBrush" Color="#9CA3AF" />

            <!-- Accent Button Style -->
            <Style x:Key="AccentButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource AmberBrush}" />
                <Setter Property="Foreground" Value="#000000" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="CornerRadius" Value="6" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border
                                x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                Padding="{TemplateBinding Padding}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="#FFCA5A" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="#CC9620" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="Root.Background" Value="#555555" />
                                                <Setter Target="Content.Foreground" Value="#888888" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <ContentPresenter
                                    x:Name="Content"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Content="{TemplateBinding Content}"
                                    Foreground="{TemplateBinding Foreground}"
                                    FontWeight="{TemplateBinding FontWeight}" />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Outline Button Style -->
            <Style x:Key="OutlineButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="Foreground" Value="{StaticResource AmberBrush}" />
                <Setter Property="BorderBrush" Value="{StaticResource AmberBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Padding" Value="16,8" />
                <Setter Property="CornerRadius" Value="6" />
            </Style>

            <!-- Converters -->
            <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
            <converters:NullToVisibilityConverter x:Key="NullToVisibility" />
            <converters:EnrichmentStatusToColorConverter x:Key="EnrichmentStatusToColor" />
            <converters:KeywordsToStringConverter x:Key="KeywordsToString" />
            <converters:IsEnrichedToColorConverter x:Key="IsEnrichedToColor" />
            <converters:IntToVisibilityConverter x:Key="IntToVisibility" />
            <converters:RatingToStarsConverter x:Key="RatingToStars" />
            <converters:StringFormatConverter x:Key="StringFormat" />
        </ResourceDictionary>
    </Application.Resources>
</Application>
